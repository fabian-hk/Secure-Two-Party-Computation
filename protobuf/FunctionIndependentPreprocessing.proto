syntax = "proto2";

package project_inf_mpc.function_independent_preprocessing;

message Bit {
    optional bytes r = 1;
    optional bytes M = 2;
    optional bytes K = 3;
}

message AuthenticatedBits {
    repeated Bit bits = 1;
}