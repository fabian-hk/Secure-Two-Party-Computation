syntax = "proto2";

package project_inf_mpc.function_independent_preprocessing;

message Bit {
    required int32 id = 1;
    optional bytes r = 2;
    optional bytes M = 3;
    optional bytes K = 4;
}

message AuthenticatedBits {
    repeated Bit bits = 1;
}